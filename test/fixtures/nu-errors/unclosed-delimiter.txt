Error: nu::parser::unclosed_delimiter

  x Unclosed delimiter.
   ,-[source:1:10]
 1 | print 'abc
   :       |  |
   :       |  `-- expected `'` here
   :       `-- unclosed — opens here (need `'`)
   `----
  help: Add a matching `'` to close this delimiter, or check that an earlier
        closer closed the wrong block.
