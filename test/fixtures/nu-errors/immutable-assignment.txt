Error: nu::parser::assignment_requires_mutable_variable

  x Assignment to an immutable variable.
   ,-[source:1:12]
 1 | let x = 1; $x = 2
   :            ^|
   :             `-- needs to be a mutable variable
   `----
  help: declare the variable with `mut`, or shadow it again with `let`
